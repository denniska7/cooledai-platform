#!/usr/bin/expect -f
# Deploy and start telemetry on .100 (ST550-Pilot-Node)
# Usage: cd /path/to/coolingai_simulator && expect scripts/deploy_to_100.exp

set timeout 60
set pass "Admin1996"
set host "cooledaiadmin@192.168.12.100"
set script_dir [file dirname [info script]]
set project_root [file dirname $script_dir]

spawn scp -o StrictHostKeyChecking=no $project_root/scripts/st550_telemetry.py $project_root/scripts/start_telemetry.sh $host:~/
expect "password:"
send "$pass\r"
expect eof

spawn ssh -o StrictHostKeyChecking=no $host
expect "password:"
send "$pass\r"
expect -re {\$ |# }
send "sudo apt-get update -qq && sudo apt-get install -y -qq python3-pynvml 2>/dev/null; sudo apt-get install -y -qq python3-pip 2>/dev/null; sudo python3 -m pip install --break-system-packages -q nvidia-ml-py3 requests 2>/dev/null; echo deps_done\r"
expect {
    "password for" { send "$pass\r"; exp_continue }
    -re {\$ |# } { }
}
expect -re {\$ |# }
send "sudo bash ~/start_telemetry.sh --node-id ST550-CooledAI-Predictive\r"
expect {
    "password for" { send "$pass\r"; exp_continue }
    -re {\$ |# } { }
    timeout { puts "Timeout"; exit 1 }
}
expect -re {\$ |# }
send "sleep 12 && tail -15 /var/log/cooledai_telemetry.log 2>/dev/null || tail -15 /tmp/cooledai_telemetry.log\r"
expect -re {\$ |# }
send "exit\r"
expect eof
