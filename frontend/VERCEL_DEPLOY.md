# Vercel deployment

- **Root Directory:** In Vercel project settings, set **Root Directory** to `frontend` (or leave empty if this repo is only the frontend).
- **Build:** Site runs in Demo Mode without authentication; no Clerk or other auth env vars required for a static build.
